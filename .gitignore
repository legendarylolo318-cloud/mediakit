/target
**/*.rs.bk
*.pdb
.DS_Store
Thumbs.db

# app data written when running locally during development
/dev-data/

# Vendored ffmpeg/ffprobe/yt-dlp downloads used to live in a workspace-local
# .vendor-cache/; build.rs now caches them under $CARGO_HOME/mediakit-vendor/
# instead (stable across `cargo clean`, and shared with everything else that
# already treats CARGO_HOME as build-artifact-ish rather than source).
